const app = (arg: string, pattern: string): boolean => {
  const a = 8;
  if (arg === pattern) {
    return false
  }
  return true
}

const arrInt: number[] = []
const simpleString: string = 'simple string'
